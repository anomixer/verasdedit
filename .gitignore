# Build output — regenerated by `node verasdedit.mjs` / build.bat
verasdedit.po

# Local test artifacts (emulator screenshots / SD card images)
*.png
*.bmp
test-sd.img

# README banner image (kept tracked despite *.png above)
!verasdedit.png
